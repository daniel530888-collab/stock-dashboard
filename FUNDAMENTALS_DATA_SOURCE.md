# 財報資料源查證結果（需求第8項的前置作業）

> **這份文件的用途**：需求第 8 項「兩層式全市場掃描 + 基本面篩選」目前只做了一半——
> 全市場掃描有了，但基本面只有「月營收年增率」一個指標，缺 ROE、負債比、毛利率、本益比。
> 動工前先查證資料源，避免寫完才發現資料抓不到或不可靠。
> 以下每一項都是**實際打過 API 驗證的結果**，不是看文件推測的。
>
> 查證日期：2026-08-06

---

## 結論先講

**可行，而且不需要 yfinance。** 證交所 OpenAPI 就有完整的財報三表與每日估值指標，
跟現有 `src/data/fundamentals.py` 用的月營收 API 是同一個體系、同樣免費、同樣免申請金鑰。

但有一個**必須先解決的架構問題**：財報端點是「當日公布增量」，不是「全市場快照」，
一次請求拿不到全市場。詳見下方第 3 節。

---

## 1. 每日估值指標 —— 可直接用，無障礙

**端點**：`https://openapi.twse.com.tw/v1/exchangeReport/BWIBBU_ALL`

**實測結果**：

```json
{"Date":"1150804","Code":"2330","Name":"台積電",
 "PEratio":"31.19","DividendYield":"0.95","PBratio":"10.21"}
```

- 涵蓋率：**全市場**（單次請求回傳 660+ 筆，含台積電等權值股）
- 更新頻率：每日
- 提供欄位：本益比（PEratio）、殖利率（DividendYield）、股價淨值比（PBratio）

這一項最單純：一次請求、全市場、每天更新，直接照 `fundamentals.py` 現有的
「打一次、建查表、所有股票共用」模式接進來即可。**建議優先做這個。**

---

## 2. 財報三表 —— 資料完整，但取得方式要注意

> **2026-08-18 更新**：下面這一節原本是 2026-08-06 查證時**看著回應用文字
> 描述**欄位，不是逐字核對的 key。2026-08-18 請使用者親自打開兩個端點、
> 貼回完整原始 JSON 後，發現有好幾個欄位名稱**跟文字敘述對不上**——
> 已在下面用刪除線標出原文、旁邊補上實測到的真實 key。
> 如果照最初的文字敘述去對 API 回傳的 dict key 寫程式，每一個欄位都會是
> KeyError 或悄悄變成 None。實作（`src/data/financial_statements.py`）
> 已經改用這裡更新後的真實欄位名稱。

### 資產負債表（一般業）
**端點**：`https://openapi.twse.com.tw/v1/opendata/t187ap07_L_ci`

實測回傳欄位（單位：仟元）：
流動資產、非流動資產、~~資產總額~~ → **資產總計**、流動負債、非流動負債、
~~負債總額~~ → **負債總計**、股本、資本公積、保留盈餘、
歸屬於母公司業主之權益合計（⚠常有空值，見下）、~~權益總額~~ → **權益總計**、
**每股參考淨值**（這個欄位名稱原本就對）

⚠ **歸屬於母公司業主之權益合計常見空值**：2026-08-18 實測資料裡，
「歸屬於母公司業主之權益合計」在不少公司（例如1342八貫、1413宏洲、
1465偉全等）是空字串，但「權益總計」仍然有值。算 ROE 分母時要用
**權益總計**，不能假設「歸屬於母公司業主之權益合計」一定有值。

### 綜合損益表（一般業）
**端點**：`https://openapi.twse.com.tw/v1/opendata/t187ap06_L_ci`

實測回傳欄位：
**營業收入**（這個對）、營業成本、
~~營業毛利~~ → 實際有兩個欄位：「營業毛利（毛損）」跟
「**營業毛利（毛損）淨額**」，兩者不同（淨額是扣掉未實現/已實現銷貨損益調整後的數字），
算毛利率要用**營業毛利（毛損）淨額**，不是前者。
營業費用、~~營業利益~~ → **營業利益（損失）**、營業外收入及支出、稅前淨利（淨損）、
~~本期淨利~~ → **本期淨利（淨損）**、~~基本每股盈餘~~ → **基本每股盈餘（元）**

### 這兩張表湊得出來的指標

| 指標 | 算法（用實測到的真實欄位名） | 需求對應 |
|---|---|---|
| 負債比 | 負債總計 ÷ 資產總計 | ✅ 使用者點名要的 |
| ROE | 本期淨利（淨損） ÷ 權益總計，季報需年化 | ✅ 使用者點名要的 |
| ROA | 本期淨利（淨損） ÷ 資產總計，季報需年化 | ✅ 使用者點名要的 |
| 毛利率 | 營業毛利（毛損）淨額 ÷ 營業收入 | ✅ 需求第8項的「毛利率改善」 |
| 營業利益率 | 營業利益（損失） ÷ 營業收入 | 加值 |
| 淨利率 | 本期淨利（淨損） ÷ 營業收入 | 加值 |
| 每股淨值 | 直接取「每股參考淨值」 | 加值 |

實作與測試見 `src/data/financial_statements.py`、`tests/test_financial_statements.py`。

---

## 3. ⚠ 關鍵發現：財報端點是「當日公布增量」，不是全市場快照

這是這次查證最重要的一件事，會直接影響實作方式。

**實測觀察（2026-08-06，財報季初期）**：

| 端點 | 出表日期 | 回傳筆數 | 含 2330 台積電？ |
|---|---|---|---|
| `t187ap07_L_ci`（資產負債表） | 全部都是 `1150803` | **51 筆**（完整 JSON 陣列，非截斷） | ❌ 沒有 |
| `t187ap06_L_ci`（綜合損益表） | 全部都是 `1150805` | 一批不同的公司 | ❌ 沒有 |

兩個端點的**出表日期不同、公司名單也不重疊**。合理的解釋是：
這兩個端點回傳的是**那一天新公布財報的公司**，不是「全市場最新一季」。

**這代表什麼**：
一次請求拿不到全市場財報。要做到全市場覆蓋，pipeline 必須
**每天抓、累積存檔、逐步補齊**，跨越整個財報公布季（約 45 天）。

好消息是這跟 NORTH_STAR.md 第三節「Raw Data 永遠不要覆蓋」的原則完全一致，
而且 repo 已經有 `data/2026/` 這種按年份分層的目錄結構可以沿用。

> **2026-08-18 追加觀測，回答上面「尚待確認」的問題**：使用者在 1150817
> （財報季接近尾聲，Q2申報法定期限前後）再打了一次這兩個端點，這次回傳的
> 是**幾乎全市場**——公司代號從 1101 涵蓋到 9958，包含 2330 台積電、
> 2317 鴻海、2454 聯發科等權值股，兩個端點都是。
>
> 這**沒有推翻** 2026-08-06 的觀察，而是補完了它：兩次觀測合起來看，
> 這兩個端點回傳的比較準確的說法是「**財報季至今，累計公布過財報的公司**」，
> 不是嚴格意義的「當日公布增量」。財報季剛開始時只有少數公司申報完，
> 批次自然小；接近法定截止日時幾乎所有公司都申報完了，批次自然趨近全市場。
> 兩次觀測都符合「單次請求不保證涵蓋全市場」這個核心結論，
> 只是「涵蓋率」本身會隨財報季進度變動，不是固定的一個數字。
>
> **實作因應**（見 `src/data/financial_statements.py`）：**不對這個行為做任何假設**——
> 不管某一天回傳的是小批次還是接近全量，一律用「upsert by 公司代號、
> 新一期（年度,季別）覆蓋舊一期」的邏輯累積進 `data/financial_statements/state.json`。
> 這個邏輯在兩種情況下都能正確收斂成「每家公司目前已知最新一期數字」，
> 不需要先確定「今天到底抓到多少比例的市場」才能動工，也不會被財報季不同階段的
> 批次大小差異影響正確性。

---

## 4. 涵蓋範圍的限制（要誠實標示給使用者看）

端點名稱結尾的 `_ci` 是**一般業**。金融、保險、證券業的財報結構不同，
走的是不同端點（`_basi`、`_bd`、`_mim` 等），欄位定義也不一樣
（例如銀行沒有「營業毛利」的概念）。

**影響**：金融股的 ROE / 毛利率會是空的。這不是 bug，是產業別的會計結構差異。
處理方式應該跟現有的 `UNKNOWN_HEALTH` 一致——**明確標示「此產業不適用」，
而不是給 0 或硬套一個數字**，否則會讓金融股在篩選時被誤判成體質差。

另外，季報是**累計數**（Q2 = 上半年累計，不是單季）。算 ROE 時要注意年化，
不能直接把季度淨利除以權益就當年度 ROE，那會低估。

---

## 5. 建議的實作順序

1. ✅ **先接 BWIBBU_ALL**（本益比／股價淨值比／殖利率）——單次請求、全市場、
   每日更新，沒有任何架構問題，做完立刻可用。已完成，見 `src/data/valuation.py`。
2. ✅ **再做財報累積機制**——2026-08-18 完成。`src/data/financial_statements.py`
   每天抓 `t187ap06/07_L_ci`，存進 `data/financial_statements/{YYYY}/{MM}/` 下的
   raw 層永不覆蓋，另外累積一份「各公司最新一期」的 `state.json` 供查詢，
   已接進 `market_analyzer.run()`（獨立 try/except，失敗不影響當天分析）。
   對應測試：`tests/test_financial_statements.py`（21項）。
3. ⏳ **最後才接進評分權重**——**這一版刻意還沒做**，`config/settings.yaml` 裡波段模式的
   `fundamental: 0.25` 目前完全靠月營收年增率一個指標撐著，
   等財報指標穩定產出後再重新分配權重，並且要重跑回測確認影響。

---

## 6. 為什麼不用 yfinance

現有 `src/data/fundamentals.py` 的檔頭已經記錄過原因：對每檔股票各打一次
yfinance 財報請求，1000 檔就是 1000 個 HTTP 請求，是觸發限流的主因。
證交所 API 是官方原始資料、免費、免金鑰，且與現有程式碼同一體系。
沒有理由為了財報再把 yfinance 拉回來。
