# 分析數據參考手冊

目前系統實際計算與輸出的所有指標、數據、判斷邏輯的完整清單。
要新增觀察指標或分析功能時，先看這份，避免重複造輪子或跟現有邏輯打架。

參數全部集中在 `config/settings.yaml`，本文標註 `⚙` 的代表該數值可在設定檔調整，不需改程式。

---

## 一、技術指標（每日對每檔股票計算）

實作位置：`src/legacy_analyzer.py` 的 `add_indicators()`

| 指標 | 欄位名 | 說明 | 目前用途 |
|---|---|---|---|
| 移動平均線 | `MA5` `MA10` `MA20` `MA60` | 收盤價的5/10/20/60日簡單移動平均 | MA20 是拉回策略的核心判斷基準；其餘目前只算不用 |
| 成交量均量 | `Vol_MA5` | 5日平均成交量 | 計算量能倍數的分母 |
| 量能倍數 | `Vol_Ratio` | 當日成交量 ÷ 5日均量 | 突破策略要求 >1.5 ⚙；拉回策略要求 <1.0 ⚙ |
| 相對強弱指標 | `RSI14` | 14日 RSI（用簡單移動平均，非 Wilder 平滑） | 動能因子評分、過熱/過冷風險提示 |
| 平均真實區間 | `ATR14` | 14日 ATR，衡量波動度 | 動態決定停損幅度 |
| 波段高低點 | `Swing_HighN` `Swing_LowN` | 近40日 ⚙ 最高/最低價 | 計算進場價在波段中的相對位置、費波那契 |
| 短波段高低 | `Swing_High20` `Swing_Low20` | 近20日最高/最低價 | 突破觸發價、拉回關注區 |

**目前算了但沒用到的：** MA5、MA10、MA60。這三條線已經在計算，
要加入策略判斷或圖表顯示的成本很低。

---

## 二、進場價與風險控管

實作位置：`src/legacy_analyzer.py`

### 驗證後進場價（`verified_entry_price`）
不是當日收盤價，而是回推出來的建議價位，算法：

1. 從歷史回測中，挑出「勝率 × 平均報酬」最高的那個策略
2. 取該策略**所有獲利交易**進場時，價格落在波段區間的平均相對位置
3. 用這個相對位置換算成今天的對應價格
4. 與費波那契 0.705 回撤位取平均，得出最終建議價

### 費波那契 0.705（內部使用，不對外顯示）
`Swing_Low + (Swing_High - Swing_Low) × 0.295`
0.705 落在 61.8%~78.6% 之間，是常用的「折價區」進場位階。

### 停損 / 停利
- 停損幅度 = `ATR14 ÷ 進場價 × 3` ⚙，再限制在 **5%~8%** ⚙ 之間
  （波動大的股票停損放寬，但不會無限放大）
- 停利 1 倍 = 進場價 + 風險金額 × 1.0 ⚙
- 停利 1.5 倍 = 進場價 + 風險金額 × 1.5 ⚙

---

## 三、策略訊號（Strategy Plugin）

實作位置：`src/strategies/`。新增策略只要在此目錄加檔案，主程式完全不用改。

### 突破型 `breakout` v1.0（風險等級：medium）
```
收盤價 > 近20日 ⚙ 最高價（不含當日）
且 量能倍數 > 1.5 ⚙
```

### 拉回型 `pullback` v1.0（風險等級：medium）
```
最低價 ≤ MA20 × 1.02 ⚙   （有觸及均線）
且 收盤價 ≥ MA20 × 0.98 ⚙  （沒有跌破）
且 量能倍數 < 1.0 ⚙        （量縮）
```

**目前就這兩個。** 這是最容易擴充的地方——新增策略不影響任何現有邏輯。

---

## 四、回測數據（每個策略 × 每檔股票）

實作位置：`src/legacy_analyzer.py` 的 `backtest()`、`src/analytics/metrics.py`

回測方式：找出歷史上所有訊號日，持有 **15天** ⚙ 後計算報酬率。

### 基本統計
| 欄位 | 說明 |
|---|---|
| `signal_count` | 歷史訊號出現次數（樣本數，低於5次 ⚙ 會標記「僅供參考」） |
| `win_rate` | 勝率 % |
| `avg_return` | 平均報酬 % |
| `avg_win_relative_position` | 獲利交易的平均進場位階（0=波段最低，1=波段最高） |

### 進階績效指標（M4）
| 欄位 | 說明 |
|---|---|
| `sharpe_ratio` | 夏普比率，每承受一單位總波動換得多少超額報酬 |
| `sortino_ratio` | 索提諾比率，只計算下跌波動（上漲波動不算風險） |
| `calmar_ratio` | 年化報酬 ÷ 最大回撤 |
| `max_drawdown_pct` | 最大回撤 %，資金曲線從高點跌下來最深幾% |
| `cagr_pct` | 年化複合成長率 |
| `recovery_factor` | 總報酬 ÷ 最大回撤，回撤修復能力 |
| `profit_factor` | 總獲利 ÷ 總虧損，>1 才是正期望值 |
| `expectancy_pct` | 每筆交易的期望報酬 % |

樣本不足或無法計算時一律回傳 `null`，不會產生 `inf` 或 `nan` 假數字。

### Walk-Forward 驗證（M5 邏輯 + M15 上線，`walk_forward` 欄位）
把歷史切成 3 段 ⚙ 連續視窗，每段獨立回測，檢查是否「大部分視窗都賺錢」
（門檻 60% ⚙）。用來分辨「策略真的有效」與「剛好踩到一段順風行情」——
`backtest` 是整段歷史當一個大水池算出來的單一數字，同一段歷史剛好有一波
大行情特別配合策略時，這個數字會太樂觀；Walk-Forward 是對同一份 `backtest`
結果的穩定性檢驗，不是另一套獨立算出來的數字。

實作位置：`src/research/walk_forward.py`（`run_walk_forward()`），M15 起由
`legacy_analyzer.safe_walk_forward()` 接進 `process_stock()`，每檔股票、
每個策略（突破型/拉回型）各算一次，寫入 `stock.walk_forward.{breakout,pullback}`：

| 欄位 | 說明 |
|---|---|
| `n_folds` | 切成幾段視窗（預設3，資料不足以切分時為 `null`） |
| `folds` | 每段視窗的期間、訊號數、勝率、平均報酬、M4進階指標 |
| `consistency.profitable_fold_ratio` | 有訊號的視窗中，幾成是賺錢的 |
| `consistency.is_consistent` | 是否達到穩定門檻（60% ⚙）；樣本不足時為 `null`，不假裝算出一個有意義的比例 |

前端顯示於 `stock.html`「Walk-Forward 穩定性驗證」區塊。

### Monte Carlo 模擬（M17 新增，`monte_carlo` 欄位）
`backtest`／Walk-Forward 看的都是「歷史交易剛好照這個順序發生」的路徑。
這裡對同一組歷史逐筆交易報酬做 **Bootstrap 重抽樣**：取後放回隨機抽出跟
歷史筆數一樣多的交易組成一條模擬路徑，重複 1000 次 ⚙，統計這些路徑的
「最終報酬」與「最大回撤」分布——量化「如果交易發生的順序不同，結果的
合理範圍有多大」，不是預測未來走勢。

實作位置：`src/analytics/monte_carlo.py`（`run_monte_carlo()`），由
`legacy_analyzer.safe_monte_carlo()` 接進 `process_stock()`，寫入
`stock.monte_carlo.{breakout,pullback}`：

| 欄位 | 說明 |
|---|---|
| `final_return_pct.{p5,p25,p50,p75,p95}` | 模擬路徑最終報酬的百分位分布 |
| `max_drawdown_pct.{p50,p95}` | 模擬路徑最大回撤的中位數／最壞情境（95百分位） |
| `probability_of_loss_pct` | 模擬路徑中「最終報酬為負」的比例 |
| `n_simulations` / `n_trades` | 模擬次數（預設1000⚙）／每次重抽的交易筆數 |

固定亂數種子（42），同一份歷史資料每天重跑會得到完全相同的模擬結果，
不會無意義地每天跳動。歷史交易筆數 < 10 ⚙ 時 `final_return_pct` 等欄位
皆為 `null`，`reason` 說明原因。前端顯示於 `stock.html`「Monte Carlo 模擬」
區塊。

### 曝險、容量與滾動表現（M23 新增，`report_metrics` 欄位）

MASTER_PLAN.md Phase 3 範圍要求的「完整報告」清單裡，最後五項在這裡補齊。
前面所有指標都在回答「賺多少、風險多大」，這五項回答的是完全不同的問題：

實作位置：`src/analytics/report_metrics.py`，由 `legacy_analyzer.
safe_report_metrics()` 接進 `process_stock()`，每檔股票、每個策略各算一次，
寫入 `stock.report_metrics.{breakout,pullback}`：

| 欄位 | 回答什麼問題 |
|---|---|
| `exposure.exposure_pct` | **有多少時間真的抱著部位。** 兩套同樣年化20%的策略，一套整年滿倉、一套只有15%時間在場上，後者明顯更好——資金其餘時間可以做別的事，承受市場風險的時間也少很多。單看報酬率完全看不出這件事 |
| `capacity.daily_capacity_twd` | **每天能吃下多少錢而不會自己把價格推走。** 回測勝率再漂亮，如果這檔一天只成交500萬，你放1000萬進去，買的當下就把價格買上去了，回測報酬不可能複製 |
| `rolling_return` / `rolling_sharpe` | **表現是穩定還是在衰退。** 整段平均值會把「前期很好、近期很差」跟「一路穩定」洗成同一個數字。跟 Walk-Forward 動機相同，但那是切成幾段各自獨立驗證，這裡是連續滑動，看得出趨勢方向 |
| `drawdown_timeline` | **痛了多久。** `max_drawdown_pct` 只給「最深跌過多少」，跌20%三筆交易就回來，跟跌20%卡了三十筆還沒回本，對能不能抱得住是完全不同的兩件事。這裡補上完整序列 + `longest_drawdown_trades`（最長連續幾筆沒回到前高）+ `current_drawdown_pct` |

刻意的實作決定：

- **Exposure 重疊訊號取聯集不重複計算**——同一天被兩筆訊號的持有期間覆蓋只算一天，
  否則會算出超過100%的假數字。計入的訊號跟 `extract_trade_returns()` 採同一套
  排除條件，確保跟其他指標算的是同一批交易。
- **Capacity 用成交金額中位數而非平均數**——成交金額分布嚴重右偏，少數爆量日
  會把平均拉高很多，用平均會系統性高估容量。而高估容量正是這個指標最不該犯的錯，
  它存在的目的就是提醒你別放太多錢進去。
- **Rolling Sharpe 直接重用 `metrics.sharpe_ratio()`**——「Sharpe 怎麼算」
  全站只有一套定義，滾動版跟整段版的年化基準相同，兩個數字可以直接比較。
- **視窗內標準差為 0 時該點回傳 `null` 而非 0**——「算不出來」跟「算出來是0」
  是不同的意思。
- **回撤序列一律非負**，跟 `max_drawdown_pct` 同一套符號約定，且時間軸的最大值
  與 M4 的 `max_drawdown_pct` 完全一致（有測試驗證這個一致性）。

參數：滾動視窗 **5筆交易** ⚙、容量參與率 **1%** ⚙、容量取樣 **60日** ⚙。
前端顯示於 `stock.html`「曝險、容量與滾動表現」區塊，回撤時間軸用行內 SVG
繪製（只需要「形狀」，為此多載入一個 Chart.js 實例並不划算）。

**已知限制（誠實記錄）**：Capacity 是「日成交金額 × 參與率上限」的粗估，
沒有考慮委託簿深度、下單節奏、市場衝擊成本模型，當**數量級參考**用，
不要當成精確的可投入金額。Exposure 沿用全站既有的「訊號可無縫依序全押」
簡化假設（跟 `win_rate`/`avg_return` 同一套），不是真實的資金佔用模擬。

### Paper Trading / Live Validation（M21 新增，獨立輸出 `docs/paper_trading.json`）

上面 `backtest`、Walk-Forward、Monte Carlo 三者本質都是「事後回頭看同一段
已經發生的歷史」，只是驗證角度不同。這裡做的事情不一樣：從「今天」開始，
不知道明天會發生什麼，逐日記錄「如果訊號出現當天真的照建議進場，之後
實際發生了什麼」——是唯一無法被「調整參數去擬合歷史」污染的驗證方式，
Phase 3（Validation Engine）最後一塊。

**範圍邊界（誠實記錄）**：現有資料源（yfinance）本來就是每日收盤後才更新，
系統本來就是「收盤後跑一次」的排程架構，這裡做的是 **EOD（收盤）模擬交易**，
不是真正即時的 Paper Trading：訊號今天觸發 → 用今天的 `verified_entry_price`
（沿用既有的驗算進場價邏輯，不是另外發明一套）當進場價；之後每天只用
「收盤價」檢查停損（`stop_loss`）／停利（`take_profit_1x`）／持有天數到期
（跟 backtest 用同一個 `hold_days` ⚙），不是模擬盤中被觸價——盤中價格路徑
對 EOD 資料源本來就不存在，假裝算得出來才是真正的問題。

實作位置：`src/trading/paper_trading.py`，由 `market_analyzer.run()` 在全市場
分析完成後呼叫（非致命，失敗不影響其餘資料照常部署）：

| 欄位 | 說明 |
|---|---|
| `by_strategy.{breakout,pullback}.n_closed` | 累積至今已平倉筆數 |
| `by_strategy.{breakout,pullback}.win_rate_pct` / `avg_return_pct` | 實盤（forward）勝率／平均報酬；已平倉筆數 < 10 ⚙ 時為 `null`，`reason` 說明樣本不足 |
| `by_strategy.{breakout,pullback}.avg_backtest_win_rate_at_entry` / `avg_backtest_avg_return_at_entry` | 這批部位「進場當下」backtest 宣稱的勝率／平均報酬均值——這才是 Live Validation 真正要比對的事：回測宣稱的表現，forward-looking 情況下有沒有兌現 |
| `open_positions[].unrealized_return_pct` | 未平倉部位用今天收盤價算出的未實現報酬 |
| `recent_closed_positions[].exit_reason` | `stop_loss`／`take_profit_1x`／`time_exit` 三選一 |

**狀態持久化**：`data/paper_trading/state.json` 是唯一需要跨日存活的檔案，
進版控（GitHub Actions 每日「提交每日快照回repo」步驟一併 commit）；
`docs/paper_trading.json` 是衍生報告，規則跟 `docs/stocks/`、`docs/health.json`
一致，不進版控。同一天執行多次（排程＋手動 `workflow_dispatch` 都可能發生）
用 `state.last_run_date` 擋掉，不會重複開平倉。

前端顯示於 `index.html`「Paper Trading · Live Validation」區塊（追蹤清單
之前），策略卡片重用策略儀表板同一套視覺語言（勝率徽章、色系），刻意讓
「勝率」這個概念在網站兩個地方看起來是同一套符號。

**已知限制（誠實記錄）**：同一檔同一策略同時只維持一個部位（不金字塔
加碼）；沒有考慮交易成本／滑價／流動性衝擊，是理想化的模擬執行；
用收盤價判斷停損/停利觸發是簡化，實際盤中價格可能已經觸價但收盤價
拉回，這會讓模擬結果比實際交易「更晚出場」。

---

## 五、基本面健康度

實作位置：`src/legacy_analyzer.py` 的 `calc_health()`，資料來自 yfinance 季報

| 欄位 | 說明 |
|---|---|
| `revenue_growth_pct` | 最新季營收 vs 前一季的成長率 % |
| `margin_change_pp` | 毛利率變化（百分點） |
| `status` / `label` | `healthy` 健康 / `watch` 觀察 / `weak` 轉弱 / `unknown` 資料不足 |

判斷規則：
- **健康** — 營收成長 > 0 且毛利率沒有衰退
- **轉弱** — 營收衰退 > 5% 或 毛利率下滑 > 3 個百分點
- **觀察** — 其餘情況

**已知限制：** 用的是「季對季」而非「年對年」，會受季節性影響（例如電子業旺淡季）。
另外只有營收和毛利率兩個維度，沒有負債、現金流、EPS、本益比等。

---

## 六、相對強弱與 Benchmark Comparison

實作位置：`calc_relative_strength()`（相對強弱）+ `src/analytics/benchmark.py`
的 `compute_alpha_beta()`（Phase 3 / M16，CAPM Alpha/Beta）

### 相對強弱（`relative_strength_pct`）
近 **20日** 個股漲跌幅 減去 指數漲跌幅：
- `relative_strength_twii` — 對台股加權指數
- `relative_strength_sox` — 對費城半導體指數

低於 **-10%** ⚙ 會列入風險提示。這個數字會被 `factor_attribution` 拿去
計分，是「贏了大盤幾%」，看不出贏的原因。

### Benchmark Comparison（`alpha_beta`，M16 新增）
用 CAPM 簡化迴歸，對「個股日報酬率」與「指數日報酬率」做線性迴歸，拆解
「贏大盤是因為承擔更高系統性風險，還是真的選股選得好」：

| 欄位 | 說明 |
|---|---|
| `beta` | 對大盤波動的敏感度。1＝跟大盤同步；>1＝比大盤劇烈；<1＝比大盤溫和 |
| `alpha_annualized_pct` | Jensen's Alpha（年化）。扣掉「純粹承擔系統性風險應得的報酬」後的超額報酬 |
| `r_squared` | 迴歸解釋力，越接近1代表個股走勢越能被指數解釋 |
| `n_obs` | 對齊後的重疊交易日數（台股與美股指數行事曆不同，只取交集） |

取最近 **120日 ⚙** 的日報酬率做迴歸，重疊天數不足20天（⚙ `MIN_OBS`）或
指數報酬率無變異時，`beta`/`alpha_annualized_pct`/`r_squared` 皆為 `null`，
`reason` 欄位說明原因——不是統計顯著性檢定，只是描述性拆解，也不是完整
學術級 CAPM（無風險利率預設0%）。前端顯示於 `stock.html`「Benchmark
Comparison」區塊。

---

## 七、綜合評分與可解釋性（M6）

實作位置：`src/analytics/factor_attribution.py`

四個因子加權得出 0~100 總分，每個因子都附帶人類可讀的理由：

| 因子 | 權重 ⚙ | 評分依據 |
|---|---|---|
| 基本面健康度 | 25% | healthy=100 / watch=60 / weak=20 / unknown=50 |
| 相對強弱 | 20% | 對大盤強弱 ±30% ⚙ 線性映射到 0~100 |
| 策略訊號品質 | 35% | 今天觸發的策略，其歷史勝率為基礎，依 Sharpe 加減分（上限 ±20 ⚙） |
| 動能與量能 | 20% | RSI 落在 50~70 ⚙ 得分最高，加上量能倍數確認 |

同時產出：
- `recommendation_reasons` — 推薦原因清單
- `risk_warnings` — 風險提示清單

### 風險提示觸發條件 ⚙
| 條件 | 門檻 |
|---|---|
| 基本面轉弱 | health = weak |
| 明顯弱於大盤 | 相對強弱 < -10% |
| RSI 過熱 | RSI > 80 |
| RSI 動能不足 | RSI < 30 |
| 流動性偏低 | 量能倍數 < 0.5 |
| 回撤過大 | 回測最大回撤 > 15% |
| 樣本數不足 | 回測訊號 < 5 次 |

---

## 八、全市場掃描（今日發現）

實作位置：`src/legacy_market_scanner.py`，資料來自證交所公開 API

**第一層技術面初篩**（一次抓全市場當日行情）
- 當日成交筆數 ≥ 300 ⚙
- 當日漲幅 ≥ 3.0% ⚙

**第二層基本面排序**
- 交叉比對月營收年增率，營收成長者優先
- 最多取 15 檔 ⚙ 做完整深度分析

---

## 九、資料品質與公司行動（Corporate Actions）

實作位置：`src/data/validation.py` 的 `validate_ohlcv()`，串接於
`src/legacy_analyzer.py` 的 `process_stock()`（追蹤清單與全市場兩條路徑
都會經過這裡，同一套驗證邏輯只寫一份）。

**已還原**：`src/data/fetch.py`、`src/data/bulk_fetch.py` 對 yfinance
一律傳入 `auto_adjust=True`，一般的股票分割、除權息會被自動還原，
不會被誤判成技術面轉弱或觸發假訊號。

**已偵測但未還原（誠實揭露，非解決）**：單日漲跌幅超過 25% ⚙ 時，
系統判定為疑似「減資」或資料源錯誤未正確還原，會寫入該檔股票的
`data_quality` 欄位，並併入 `score.risk_warnings` 顯示在網站上。
減資對台股是常見的公司行動，但 yfinance 對它的還原不穩定，
沒有可靠的免費資料源能完整處理，所以只做到「偵測並明確標示」，
不假裝已經修正——這跟存活者偏誤（Survivorship Bias）採用同一套處理原則。

**已知不一致（尚未修復）**：`legacy_analyzer.calc_health()`（追蹤清單路徑，
目前因 `config/settings.yaml` 的 watchlist 為空而處於未使用狀態）仍用
「季對季」計算營收成長率，跟 M10 全市場路徑（`src/data/fundamentals.py`，
已改用「年對年」YoY）不一致。目前不影響正式站（追蹤清單是空的），
但如果之後重新在 config 加回追蹤清單，這條路徑會重新啟用舊的季對季邏輯，
屆時需要一併修正成呼叫 `fundamentals.py` 的 YoY 版本。

---

## 十、公司事件追蹤（Corporate Events，M19 新增，`corporate_events` 欄位）

MASTER_PLAN.md Phase 1 最後一塊缺口：「真正的公司事件追蹤（財報/法說會/
減資的結構化事件資料，不只是價格異常偵測）」。

**實際查證(2026-08-04)**：直接呼叫 `openapi.twse.com.tw` 驗證欄位後確認，
證交所 OpenAPI **沒有**減資、法人說明會的專屬結構化資料表——這兩項在
公開資訊觀測站上是人工公告/PDF形式，沒有開放API，只散落在「重大訊息」
的自由文字說明裡。這裡誠實採用實際存在、且驗證過欄位正確的兩個資料源，
不假裝有完整的事件資料庫：

實作位置：`src/data/corporate_events.py`，由 `market_analyzer.
analyze_universe()` 全市場批次取得一次(避免654檔逐檔打API)，或
`legacy_analyzer.process_stock()` 未被注入時自行取得(供WATCHLIST路徑
使用)，寫入 `stock.corporate_events`：

| 子欄位 | 資料來源 | 說明 |
|---|---|---|
| `upcoming_exdividend` | 除權除息預告表(`TWT48U_ALL`) | 離今天最近的一筆除權/除息事件,含日期/類型/現金股利/股票股利比例;沒有事件時為`null` |
| `recent_disclosures` | 每日重大訊息(`t187ap04_L`) | 最近一次抓取到的重大訊息快照(更名、訴訟、董事會決議、轉投資等);只是「當天」快照,不是可查詢區間的歷史資料庫,沒有公告時為空陣列 |

**與M14資料品質偵測串接**：`corporate_events.annotate_price_anomaly()`
在 `process_stock()` 裡把兩件事接起來——`validate_ohlcv()` 偵測到疑似
未還原的價格異常時，如果剛好有除權息事件落在附近 10 天 ⚙ 內，會在
`data_quality.issues`（進而併入 `score.risk_warnings`）附加一句「可能與此
有關」的說明，而不是讓兩個獨立警訊各說各話，使用者要自己猜有沒有關聯。
不會改變 `is_valid` 判定本身——這只是解釋，不是消除警訊。

前端顯示於 `stock.html`「公司事件」區塊，刻意只在有資料時才渲染
（多數股票在多數日子都沒有事件，全部顯示會製造650檔裡九成以上是空區段
的視覺雜訊）。

**已知限制（誠實記錄）**：沒有減資、法人說明會的結構化事件資料，證交所
OpenAPI 不提供；`recent_disclosures` 只是快照不是歷史查詢，需要完整歷史
時仍要去公開資訊觀測站查。

---

## 十一、今日市場環境（Market Regime，M18 新增，`market_regime` 欄位）

MASTER_PLAN.md 點名的「目前最大的單一缺口」：網站對每一檔股票都算了一堆
指標，但完全沒有告訴使用者「今天整體大盤環境是什麼樣子」——同樣一個突破
訊號，在多頭格局下追價跟在空頭格局下追價，合理的積極程度完全不同。

實作位置：`src/analytics/market_regime.py`（`compute_market_regime()`），
用**台股加權指數**（不是費半——費半是產業對照指數，不適合當作「台股整體
環境」的判斷基準）自己的價格結構做兩個維度分類：

| 維度 | 判斷依據 | 分類 |
|---|---|---|
| 趨勢（`trend`） | 收盤價相對 20日 ⚙／60日 ⚙ 均線的排列 | 多頭（收盤價>MA20>MA60）／空頭（收盤價<MA20<MA60）／盤整（其他） |
| 波動度（`volatility`） | 近20日 ⚙ 年化波動率 ÷ 近120日 ⚙ 基準波動率 | 偏高（比例≥1.3⚙）／偏低（比例≤0.7⚙）／正常（其他） |

`risk_note` 欄位針對 3×3=9 種組合各附一句白話決策提示（Phase 6「五分鐘
完成今日決策」的精神），例如「空頭格局加上波動放大，是風險最高的組合，
建議嚴格控管部位與停損」。

刻意的簡化與誠實揭露：只用價格結構判斷，不含總經數據（利率、匯率、資金
流向），是技術面的簡化版 Market Regime，不是嚴謹的總經模型；指數資料
不足（例如抓取失敗、歷史筆數不夠算 60日/120日均線）時，`trend`／
`volatility` 皆為 `"unknown"`，`risk_note` 為 `null`，不假裝算出一個看似
合理、實則沒有根據的分類。

這是 `legacy_analyzer.main()` 的輸出（`docs/data.json` 的 `market_regime`
欄位），跟 `WATCHLIST` 是否配置無關——大盤指數資料本來就一定會抓，不需要
額外請求，即使追蹤清單是空的（M13後的正常狀態）也會照常計算。前端顯示於
`index.html` 首頁最上方的「今日市場環境」橫幅。

---

## 十二、今日決策摘要（Today's Risk/Opportunity，M20 新增）

MASTER_PLAN.md Phase 6「決策中心」點名的缺口：網站對每一檔股票都算得出一堆
指標，但使用者要逐檔翻 650 檔才找得到「今天最值得看的機會」跟「觀察清單裡
要注意的風險」——跟 Market Regime（第十一節）解決的是同一種「不用逐檔翻，
先看這裡」的問題，差別是 Market Regime 給的是大盤層級的一句話，這裡給的是
「個股層級、當天就要看」的清單。

純前端功能（不需要新的 Python/後端資料，全部從已載入的 `index/stocks.json`
與 `data.json` 的 `market_regime` 欄位現算），實作位置：`docs/index.html`，
渲染於首頁最上方（Market Regime 橫幅之後、追蹤清單之前）的「今日決策摘要」
區塊，分兩欄：

### 機會（`computeTopOpportunities()`）
全市場（約650檔，不受追蹤清單限制）中，篩選條件：
- 當日有觸發突破或拉回訊號（`breakout_signal` 或 `pullback_signal`）
- 基本面健康度不是「轉弱」（`health_status !== 'weak'`）——訊號再強，
  基本面已知轉弱的不該推薦
- 有總分（`total_score` 不是 `null`）

依 `total_score` 由高到低排序，取前 5 檔。

### 風險（`computeWatchlistRisks()`）
只看使用者自己的觀察清單（不是全市場），對每一檔標記出風險原因：
- 基本面轉弱（`health_status === 'weak'`）
- 單日跌幅達 5% ⚙ 以上（`change_pct <= -5`）

同時疊加大盤層級風險：`market_regime.risk_note` 存在、且 tone 為
`caution`／`danger`（空頭或高波動組合）時，在風險欄最上方額外顯示一句
大盤層級提示，跟個股層級的風險並列，不互相取代——即使觀察清單裡沒有任何
一檔個股觸發風險條件，只要大盤環境本身值得留意，也不會顯示「沒有風險」
的空狀態。

機會清單與風險清單都是「有資料才顯示，沒資料就顯示明確的空狀態文字」，
不強行湊出內容製造虛假的緊迫感。三個會改變機會/風險內容的動作（首頁
渲染、全市場索引載入完成、觀察清單增減）都會觸發重新計算，不需要重新整理
頁面。

**已知限制（誠實記錄）**：機會清單的排序只看 `total_score`（第七節綜合
評分），沒有額外的「今天特別值得注意」的加權邏輯；風險清單目前只有兩個
觸發條件（基本面轉弱、單日大跌），沒有涵蓋 Walk-Forward 不穩定、Monte
Carlo 高虧損機率等更細緻的風險維度，之後可以視需要擴充判斷條件。

---

## 十三、警報中心（Alert Center，M22 新增）

MASTER_PLAN.md Phase 6「決策中心」的另一塊缺口。跟第十二節「今日決策摘要」
的分工要講清楚：**那邊是系統依固定規則挑給你看的**（有訊號、基本面非轉弱、
依總分排序前5檔），規則寫死在程式裡；**這邊是使用者自己定義的條件**，
系統只負責每天比對並告訴你哪些成立了。兩者都在回答「今天我需要知道什麼」，
但來源與可控性完全不同。

純前端功能（無後端資料），實作位置：`docs/index.html`，渲染於首頁
「今日決策摘要」之後、Paper Trading 之前。

### 支援的五種警報條件

| 類型 | 觸發條件 | 需要數值 |
|---|---|---|
| `price_above` 價格突破 | 收盤價 ≥ 設定價位 | ✓（元） |
| `price_below` 價格跌破 | 收盤價 ≤ 設定價位 | ✓（元） |
| `drop_pct` 單日跌幅達 | 當日漲跌幅 ≤ -設定% | ✓（%） |
| `signal` 出現進場訊號 | 當日觸發突破型或拉回型訊號 | ✗ |
| `health_weak` 基本面轉弱 | `health_status` 轉為 `weak` | ✗ |

每筆觸發都附上白話原因（例如「收盤 1000 元，已達設定的 900 元」），
不是只亮一個紅點讓使用者自己去查為什麼。

### 刻意的設計決定與誠實揭露

- **只存 localStorage**：警報是「使用者自己的設定」而非全站資料，
  沒有後端（見 `MASTER_PLAN.md` 架構決策 B）之前不該假裝能跨裝置同步。
  這跟觀察清單採用同一套處理原則。
- **不是推播，是「打開網站時比對」**：靜態網站沒有背景執行能力，
  說成「即時提醒」會是誇大功能。條件在收盤資料更新後才會被比對到。
- **查不到該檔資料時跳過不評估**，不會判定為「沒觸發」——「不知道現況」
  跟「確認過沒觸發」是兩件事，索引載入失敗時不該讓使用者以為條件安全。
- **新增失敗時說明可能原因**：代號查不到時區分「索引讀取失敗」與
  「不在分析範圍內（ETF、成交量不足）」，不含糊地說「找不到」讓使用者
  以為是自己打錯。
- **localStorage 資料損毀或型別不對時降級為空清單**，不讓整頁掛掉。

**已知限制（誠實記錄）**：只支援上述五種條件，沒有組合條件（AND/OR）、
沒有 RSI/量能/Walk-Forward 穩定性等更細緻的觸發維度；沒有觸發歷史紀錄
（只顯示「今天」的狀態，昨天觸發過但今天不成立的不會留下痕跡）；
沒有 Email/推播通知，需要主動打開網站才看得到。

---

## 十四、交易模式與風險管理（M27-M29 新增，`trading_modes` / `risk_report`）

依《交易策略與風險管理系統設計規範》建立。核心理念:**同一檔股票在不同
持股週期下,該看的指標、能承受的波動、合理的停損停利完全不同**。與其給
一個通用分數讓使用者自己換算,不如每個模式各自定義一套標準。

### 已上線的兩個模式

| | 短線交易 | 波段交易 |
|---|---|---|
| 持股期間 | 1～10個交易日 | 2～12週 |
| 每筆風險 ⚙ | 1.0% | 1.5% |
| 止損 ⚙ | 2～3% | 3～5% |
| 停利 ⚙ | 4～8% | 10～20% |
| 最低風險報酬比 ⚙ | 1:2 | 1:3 |
| ADX 門檻 ⚙ | ≥20 | ≥25 |
| 量能爆量確認 ⚙ | 必要 | 不要求 |
| 五維權重(趨勢/動能/量能/基本面/風險) | 20/30/25/**10**/15 | 30/20/10/**25**/15 |

基本面權重的差異是刻意的:短線幾天內出場,財報好壞影響有限;波段抱數週,
基本面支撐才撐得住。

**長期投資模式尚未實作**——它需要 ROE/ROIC/EPS CAGR/自由現金流/毛利率/
營益率/負債比/PEG/P-E/P-B/殖利率等十餘項財務指標,而本站目前的基本面
**只有月營收年增率一項**。硬做會產出一個看起來專業、實際上在猜的評分。
這是刻意的範圍邊界,等財報資料源確定後再加。

### 五維評分與硬性篩選的分工

`trading_modes.<mode>.dimensions` 是五維評分(每維含分數、權重、**可讀理由**);
`passes_filters` 是硬性條件(ADX 趨勢強度、量能倍數、爆量確認)。

兩者刻意分開:**硬性條件不該被高分補償**。短線在盤整期(ADX<20)做突破
本來就是高失敗率的行為,不該因為其他維度分數高就讓它出現在推薦清單裡。
未通過時 `filter_reasons` 列出具體原因,不是只給一個 false。

### 建議價位（`levels`）

依模式的止損停利區間 + 該檔 ATR 波動度動態決定:波動大的止損放寬(否則
容易被正常波動掃出場),但**一律限制在模式設定的區間內**——短線就不該
出現 8% 的止損,那已經不是短線的風險預算了。同時輸出風險報酬比與
`meets_min_rr`(是否達到該模式門檻)。

### 新增技術指標（M27，`src/analytics/indicators.py`）

EMA、MACD(DIF/DEA/柱狀體)、ADX(+DI/-DI)、布林通道(含 `BB_WIDTH` 通道
寬度,收窄常是變盤前兆)、KDJ、Momentum、Donchian 通道、量能爆量偵測
(`Vol_Ratio20` / `VOL_SPIKE`)、Volume Profile 的 POC。

**刻意沒做 VWAP**:規範的短線模式列了 VWAP,但它需要盤中逐筆或分鐘級資料,
本站只有日線 OHLCV。用日線算出來的「VWAP」只是收盤價的變形,不是真正的
VWAP——放上去會讓使用者以為看到盤中均價位置,比不做更糟。

**Volume Profile 是簡化版**:真正的版本用盤中逐筆成交價分佈,這裡等於假設
「當天成交量全部發生在收盤價」。能看出籌碼大致集中的價位帶,不該當成
精確的委託簿結構。

### 風險決策指標（M29，`src/analytics/risk.py`，`risk_report` 欄位）

跟 `metrics.py` 的分工:那邊算「這個策略歷史表現如何」(績效),
這邊算「我該投入多少、最壞會怎樣、還該不該做」(決策)。

| 欄位 | 說明 |
|---|---|
| `r_multiple` | R倍數。止損3%時獲利9%=+3R。**只看勝率會做出完全相反的決策**——勝率70%但平均賺0.5R賠1R是虧錢的 |
| `expected_value` | 期望值。`is_tradeable` 為 false 時標示「不建議交易」——負期望值長期執行必虧,這是數學不是運氣 |
| `kelly` | 凱利公式。預設 **Half Kelly**(`kelly_fraction: 0.5` ⚙),因為 Full Kelly 假設勝率與賺賠比完全準確,實際上是有限樣本的估計值,高估一點就會過度下注 |
| `var` | VaR 與 CVaR,用**歷史模擬法不假設常態分佈**——股票報酬尾部比常態厚,用常態會系統性低估極端風險 |
| `drawdown_status` | 回撤分級(10/20/30% ⚙)與建議動作。**只回報不自動改部位**——自動降部位屬執行層,在沒有真實下單串接前該做的是明確告知 |

**凱利的安全機制**:交易樣本 < 20 筆時直接回傳 `null`。樣本少時凱利常算出
危險的高比例(例如40%),照做會毀掉帳戶——這種時候「算不出來」比
「算出一個危險的數字」安全得多。凱利為負時也不做下限截斷,如實顯示負值
並說明「此策略不具正期望值,不應投入資金」。

### 建議入場金額（M30，`trading_modes.<mode>.position_sizing`）

規範第6節。**部位大小是整個系統裡唯一直接決定「你會虧多少錢」的數字**——
選錯股票虧的是一筆,部位算錯是每一筆都錯。所以每個方法除了結果之外,
一律輸出 `formula`(公式)、`steps`(代入實際數字的逐步計算)、
`caveat`(什麼情況下會失準),讓使用者能自己驗算。

實作位置:`src/analytics/position_sizing.py`

| 方法 | 公式 | 說明 |
|---|---|---|
| **固定風險%**(基準) | 資金比例 = 每筆風險% ÷ 停損幅度% | 每筆最壞情況一致。停損遠的買少、近的買多 |
| **ATR 部位** | 停損距離 = ATR × 倍數 ⚙,再套固定風險% | 純用波動定義風險距離,不受模式止損區間約束 |
| **波動度部位** | 資金比例 = 目標年化波動% ⚙ ÷ 個股年化波動% | 讓每個部位貢獻的波動一致(risk parity 單資產版) |
| **凱利公式** | 沿用 `risk.kelly_criterion()` 的結果 | 公式單一事實來源在 `risk.py`,這裡不重算 |
| **動態部位**(推薦) | 固定風險%基準 × 大盤乘數 × 回撤乘數 × 篩選乘數 | 見下 |
| 固定金額 / 固定股數 | 直接設定 | 列出供對照,但明確標示**風險不受控、不建議採用** |

**動態部位的乘數**(把系統既有的三個判斷真的接進部位大小,而不只是顯示給人看):

```
大盤空頭        × 0.5     空頭裡逆勢做多本來就該減碼
大盤高波動      × 0.7     波動放大時同樣部位的實際風險更大
回撤 elevated   × 0.7
回撤 high       × 0.5
回撤 severe     × 0       已經傷得夠重,停止新倉
未通過模式篩選  × 0       硬性條件沒過就不該進場
```

**乘數相乘而非取最小值**:多個不利條件同時成立時,風險是疊加的。

**資金無關的設計**:使用者本金存在 localStorage,Python 不知道也不該知道。
所以輸出的是**資金比例**,前端只做最後一步乘法:

```
建議投入金額 = 本金 × capital_fraction
建議股數     = floor(建議投入金額 ÷ 進場價)      台股零股可買,不湊整張
```

公式與參數的單一事實來源留在 Python,前端只代入本金。

**兩個模式對同一檔會給出不同金額**——短線每筆風險 1.0%、波段 1.5%,
且各自的停損價不同,這正是分模式的意義。

**驗算範例**(有測試鎖住這個一致性):進場 100、停損 95、每筆風險 1%
→ 停損幅度 5% → 資金比例 = 1%÷5% = 0.2。本金 100 萬投入 20 萬買 2000 股,
觸及停損虧 2000×5 = 1 萬 = 本金的 1%,與設定完全相符。

前端顯示於 `stock.html`「建議入場金額」區塊:推薦方法的金額為主視覺,
其餘方法收在可展開的對照區;每個方法都能展開看公式與逐步計算;
算不出來的方法(例如凱利樣本不足)顯示具體原因而非硬給數字;
動態部位歸零時明確顯示「建議不進場」而非顯示 0 元讓人困惑。

### 前端

首頁封面提供模式選擇卡片,選擇存於 localStorage。切換模式後「今日決策摘要」
的排序與篩選都改用該模式的標準,清單顯示的是**該模式評分**而非綜合分數
(顯示的數字就是排序依據,避免誤導)。索引尚未重新產生(無 `modes` 欄位)時
自動退回綜合分數排序,網站不會空掉。

**已知限制（誠實記錄）**:各模式的回測仍共用全站的 15 天持有期,還沒有
依模式週期各自回測(短線5天 vs 波段30天),所以風險維度參考的回測數據
對兩個模式是同一份;缺籌碼面資料(法人買賣超、融資券),規範列的這兩項
未納入評分;缺產業分類,無法做產業趨勢與同業比較;無新聞情緒資料,
規範的 Sentiment Score 未實作。

---

## 十五、我的模擬資產（Portfolio，M25 新增，純前端）

把「策略驗證裡實際發生的交易」用**你自己的本金**重新跑一次，回答
「如果當初照這套規則用我的錢做，今天會是多少」。

### 架構分層（這是本功能最重要的設計決定）

| 層 | 負責什麼 | 存在哪 |
|---|---|---|
| Python（`paper_trading.py`） | **發生了哪些交易**——對全市場所有訊號開倉，不受資金限制，因為策略驗證要的是完整樣本 | `data/paper_trading/state.json`（進版控，全站共用） |
| 前端（`index.html`） | **那是多少錢**——你的本金、風險偏好、手續費 | localStorage，只在這台瀏覽器，不會 commit 進 repo |

兩層不互相污染：驗證統計不會因為你本金多寡而失真，你的資產數字也不會被
推上 GitHub。這也是為什麼這個功能**不需要後端**。

### 部位大小：固定風險%

每筆交易最多虧掉本金的 `riskPct%`，依該檔的停損距離反推股數：

```
每股風險 = 進場價 − 停損價
可買股數 = min( 本金 × riskPct% ÷ 每股風險 ,  可用現金 ÷ 進場價 )
```

停損遠的自動買少、近的買多，讓**每筆的最壞情況一致**。台股零股可交易，
所以算到「股」不湊整張。停損距離無效（等於或高於進場價）時回傳 0 不硬買。

### 資金有限時挑哪幾檔

依 **`score_at_entry`（進場當下的綜合分數）** 由高到低填補空位，受
`maxPositions` 與現金雙重限制。用進場當下的分數而不是今天的分數——
事後用今天的分數去挑當初該買哪檔，那是未來資訊，會讓回放結果好看到不真實。

沒買到的筆數會顯示為 `nSkipped` 並明講「這正是資金有限的真實狀況，不是漏算」。

### 交易成本（預設計入，可調）

| 項目 | 預設 | 說明 |
|---|---|---|
| 買進手續費 | 0.1425% | 台股公定價，券商常有折扣可自行調低 |
| 賣出手續費 | 0.1425% | 同上 |
| 賣出證交稅 | 0.3% | 台股現股 |

這是刻意的：既然要用「我的錢」算，就不該是零成本的理想模擬。有測試專門
驗證「有成本的損益確實低於無成本」。

### 其他實作決定

- **回放而非乘法**：不是把報酬率乘上本金，而是依時間順序重跑。因為資金
  有限，某天同時有20個訊號但只買得起3檔，買了哪3檔會影響後面每一筆的
  可用資金，不回放就算不對。
- **同日先賣後買**：當天平倉拿回的錢，當天可以再投入，跟真實交易一致。
- **未平倉估值**：有今日報價就用今日價，**查無報價時用成本價**不亂猜一個
  數字讓總資產失真。
- **未設定本金時不顯示任何金額**：不拿預設值假裝是你的錢。
- **平倉歷史被截斷時明確告知**會有落差（見 `paper_trading.max_closed_history` ⚙）。

**已知限制（誠實記錄）**：沒有考慮滑價與市場衝擊；沒有產業集中度、
相關係數矩陣等組合層風險控管（Phase 4 剩餘範圍，詳見下方 Portfolio Heat
小節）；設定只存單一瀏覽器，換裝置要重設（沒有後端之前不假裝能同步）。

### Portfolio Heat + Market Regime 連動曝險建議（2026-08-18 新增）

`computePortfolioHeat(openHoldings, capital)`：如果**全部**未平倉部位
「同時」都跌到停損價，總共會虧掉本金的百分之多少。`positionShares()`
的 `riskPct%` 只限制單筆風險，管不到「全部同時出事」——尤其大盤系統性
下跌時，持有的股票反而最容易一起觸發停損，機率不是零。

```
單筆風險 = 股數 × max(0, 進場價 − 停損價)
Portfolio Heat = Σ單筆風險 ÷ 本金 × 100%
```

上限**不用產業慣例的憑感覺數字**（例如常聽到的「總風險不超過20%」），
而是直接從使用者已經同意的兩個設定值相乘算出來：`riskPct% × maxPositions`。
這是使用者自己定義過的「每筆最多虧多少、最多同時開幾筆」的自然乘積，
不是另外發明一個要重新理解的新門檻。

`marketRegimeExposureAdjustment(mr)`：依今日 Market Regime 把這個上限打折——
空頭+高波動同時出現打對折（0.5）、單一風險因子（只空頭或只高波動）打8折（0.8）、
環境正常維持1.0。**只給建議、不會自動改變已經算好的部位大小**——加碼與否
是使用者的決定，系統只負責在危險組合下明講「現在不是加碼的時候」。

超過上限時，「我的模擬資產」分頁的 pf-summary 會標紅並附文字說明。
測試見 `tests/test_index_portfolio_js.js` 情境15-19。

### 產業集中度（2026-08-18 新增）

資料來源：`src/data/industry_classification.py`，抓
`https://openapi.twse.com.tw/v1/opendata/t187ap03_L`（上市公司基本資料）
取得每家公司的產業別代碼，對照
`https://isin.twse.com.tw/isin/class_i.jsp?kind=1` 的官方代碼表轉成中文
名稱（兩個來源互相交叉驗證過，見該檔案檔頭）。**只萃取公司代號跟產業別
兩欄**，來源端點裡的董事長、電話、電子郵件等個資欄位一律丟棄，不落地
保存也不往下游傳遞。

產業別接進全市場搜尋索引（`docs/index/stocks.json` 的 `industry_name`
欄位）而不是另開新檔——前端本來就會把這份索引整包載入
`MARKET_INDEX_BY_CODE` 供搜尋使用，`computeIndustryConcentration()`
直接查表即可，不必再多發一次請求。

```
單一產業佔比 = 該產業持股市值加總 ÷ 全部持股市值加總 × 100%
```

超過 50%（字面意義的「過半數」，不是另外調查的業界慣例數字）時，
pf-summary 顯示警告。查不到產業別的股票歸類「未分類」，不會讓整體
計算出錯或漏算該檔市值。測試見 `tests/test_industry_classification.py`
（9項）與 `tests/test_index_portfolio_js.js` 情境20-22。

### 相關係數矩陣（2026-08-18 新增）

產業集中度管的是「帳面上分類成不同產業」，但同產業以外的股票也可能漲跌
高度連動（上下游關係、同受匯率影響的出口股）——分屬不同產業、實際上
卻是同一個風險來源，產業集中度看不出來，相關係數才看得出來。

用每檔持股 `docs/stocks/<code>.json` 裡現有的 `history`（近60個交易日
收盤價，`legacy_analyzer.py` 輸出，本來就有，不必新增資料源）換算日
報酬率，兩兩配對算 Pearson 相關係數。對齊方式用「日期交集」，跟
`src/analytics` 算 alpha/beta 用的是同一個原則（見
`tests/test_benchmark.py` 情境4：不同交易日曆只取交集，不強行套用不
對齊的資料），重疊天數門檻（20天）也直接沿用該處的判斷。|r|≥0.7 是
統計學教科書常見的「高度相關」門檻，不是本站發明的業務規則。

只在使用者查看「我的模擬資產」分頁、且持股數 ≥2 檔時才對外抓取
（`renderPortfolioCorrelation()`），非同步進行、不擋住其餘畫面；用持股
組合(排序後的代號清單)當快取 key,持股沒變時不重複抓取。

**2026-08-18 實作時抓到的一個真實 race condition**：`renderPortfolio()`
在切分頁、存設定等情境下會頻繁重跑，每次都會 fire-and-forget 呼叫
`renderPortfolioCorrelation()`。最初的版本只用「持股組合的 key 是否相同」
判斷結果是否過期,但三個以上請求交錯完成時(例如 A→B→A 依序發出、B
比較慢,最後 resolve 的反而是第二個 A)第二個 A 的 key 剛好又等於自己
出發時的 key,會被誤判成「還是最新的」而把 B 的結果蓋回去。改用嚴格
遞增的 `_correlationRequestSeq`,只有全域最新一次發出的請求才能真的
寫入畫面,不管幾個請求怎麼交錯完成。這個 bug 是被
`tests/test_index_portfolio_js.js` 情境27(刻意在呼叫 CORR1/CORR2 測試
前先觸發一次不同持股組合的 render)抓到的,不是憑空猜測要修。

測試見 `tests/test_index_portfolio_js.js` 情境23-28。

### 流動性上限接進部位大小（2026-08-18 新增,Phase 4 收尾)

前面幾項(Portfolio Heat、產業集中度、相關係數矩陣)管的都是「整個組合」
層級的風控。這一項不同,是「單筆」層級——即使風險預算跟現金都夠,買進
金額如果佔某檔股票單日成交金額比例過高,自己的買單就會把成交價墊出去,
實際成交結果會比回測算出來的差(市場衝擊)。

**容量指標本來就有,只是沒接進部位大小。** `src/analytics/report_metrics.py`
的 `capacity_estimate()` 早就用「近60個交易日成交金額中位數(用中位數避免
單日爆量的離群值把容量高估)× 參與率」算出每檔股票的
`daily_capacity_twd`(參與率是 `capacity_participation_rate_pct`,
`config/settings.yaml` 既有值,預設1%,不是這次臨時發明的數字),而且
每檔的 `report_metrics.breakout/pullback.capacity` 底下本來就有這個欄位——
只是前端的 `positionShares()` 從來沒有用它限制股數。

**資料路徑**:`src/market_analyzer.py` 的 `build_index_entry()` 從
`report_metrics.breakout.capacity`(拿不到時退而求其次拿
`pullback.capacity`——兩種交易模式共用同一份 df,理論上容量算出來相同)
萃取 `daily_capacity_twd`,放進全市場搜尋索引(`docs/index/stocks.json`)
的 `capacity_daily_twd` 欄位。前端已經整份索引載進 `MARKET_INDEX_BY_CODE`
供搜尋使用,查表即可,不必再多發一次請求——跟產業別欄位是同一個做法。

**套用方式**:`positionShares()` 新增第七個參數 `dailyCapacityTwd`,在
既有的 `byRisk`(風險預算推算)、`byCash`(現金上限)之外,再加一道
`byCapacity = Math.floor(dailyCapacityTwd / entryPrice)`,三者取最小值。
`dailyCapacityTwd` 為 `null`/`0`(查無資料,例如該股成交量資料不足)時
不設這道上限,不因為索引缺欄位而擋住原本有效的交易。兩個呼叫端都接上:
`simulatePortfolio()`(「我的模擬資產」回放歷史交易)、
`resolveMyPendingEntries()`(「我的資產(手動帳戶)」排隊成交判定)。

**一個刻意的簡化,寫進程式碼註解**:用的是「現在」查到的容量去套用在
(可能是歷史上的)這一筆進場——前端架構沒有逐日的歷史容量快照可查
(全市場索引只保留當下這一份)。這樣做寧可在流動性真的變差的個股上
偏保守一點,也不假裝精確到「進場當天」的真實容量。

**透明度**:`simulatePortfolio()` 額外算一次「拿掉容量限制」的股數跟
實際股數比較(不重複實作 `byCapacity` 的公式,直接借 `positionShares()`
自己的邏輯來比,兩邊一旦公式改了也會自動保持一致),算出 `nLiquidityCapped`
(被流動性壓低股數的筆數)。`renderPortfolio()` 在這個數字大於0時顯示
提示,跟「完全沒買到」(`nSkipped`,對應「已達最大持股數」或「現金不足」)
分開講——被流動性壓低股數不等於沒進場,混在一起講會讓使用者誤以為
整筆訊號被跳過了。

測試見 `tests/test_market_index_entry.py`(6項,涵蓋
`build_index_entry()` 的容量萃取與 breakout/pullback 退而求其次邏輯,
順便補了產業別欄位當初沒留下的專用測試)、
`tests/test_index_portfolio_js.js` 情境3b(`positionShares()` 容量上限
單元測試)、22b(`simulatePortfolio()`/`renderPortfolio()` 整合測試)、
`tests/test_index_my_account_js.js` 情境2b(T7 手動帳戶排隊成交的
流動性上限)。

Phase 4 Portfolio Engine 五項子任務(Portfolio Heat、Market Regime連動、
產業集中度、相關係數矩陣、流動性過濾)全部完成。

---

## 十六、近期重要日程（Economic Calendar，M24 新增，`economic_calendar` 欄位）

Phase 6 的最後一塊。前面三塊都在講「今天」——Market Regime（第十一節）講
今天大盤環境、今日決策摘要（第十二節）講今天該看哪幾檔、警報中心（第十三節）
講你設的條件今天成立了沒。這裡補上時間維度：**接下來有哪些已知的日子會影響盤面**。

實作位置：`src/data/economic_calendar.py`，由 `legacy_analyzer.main()` 產生，
寫入 `docs/data.json` 的 `economic_calendar` 欄位，前端顯示於 `index.html`
「近期重要日程」區塊。預設往前看 **60天** ⚙。

### 三類資料來源（每個事件都標註 `source_type`）

可靠度本來就不同，所以在畫面上明確標示，使用者有權知道看到的這一條是哪來的：

| `source_type` | 內容 | 可靠度 |
|---|---|---|
| `statutory` 法規推算 | 月營收公布截止（每月10日）、各季財報公布截止（3/31年報、5/15 Q1、8/14 Q2、11/14 Q3） | 最高。這些是**規則不是資料**，用日曆推算即可，不依賴任何 API——規則本身比任何資料源都穩定 |
| `market_data` 證交所資料 | 除權息集中日（同一天達 **5檔** ⚙ 以上才列出） | 高。重用 M19 已抓的全市場除權息預告，是真實資料 |
| `manual` 人工設定 | 央行理監事會議、FOMC 等重要會議 | 需定期維護。設定在 `config/settings.yaml` 的 `economic_calendar.manual_events` |

### 為什麼人工清單預設留空（誠實記錄）

2026-08-05 實際查證後確認：主計總處/國發會的總經數據發布時程、央行與 FOMC
的會議日程，**都沒有免費且穩定的結構化 API**（`data.gov.tw` 的資料集查詢 API
實測回傳空內容）。

刻意**不預先填入未經查證的日期**——填錯的行事曆比空的行事曆更糟，使用者會
照著一個錯的日子做決策。所以：

- 清單留空時，畫面明確顯示「這類事件尚未設定」並說明去哪裡補
- 清單裡的日期**全部過期**時，畫面顯示「已過期（最後一筆是 YYYY-MM-DD），
  需要更新」並用警示樣式——不會安靜地呈現一個看起來完整、實際上停在半年前
  的行事曆
- 這兩種狀態在視覺上明確區分（未設定是中性提示，過期是警示）

要啟用的話，到央行（cbc.gov.tw）與 Fed（federalreserve.gov）官網查到當年度
會議日期後，照 `config/settings.yaml` 裡的註解格式填入即可。

### 其他實作決定

- **除權息只列集中日不列個別股票**：除權息旺季每天都有零星幾檔，全部列出來
  會把行事曆洗版成雜訊。這裡回答的是「哪幾天除權息特別密集」，個別股票的
  除權息日在個股頁面的「公司事件」區塊（第十節）已經有了。
- **單一來源失敗不影響其餘**：三類事件各自獨立處理，除權息資料結構異常時
  法定期限照常呈現（有測試驗證這個行為）。
- **跨年正確處理**：12月看得到隔年3/31的年報截止日，且跨年的月份命名正確
  （1月10日公布的是「12月營收」）。
- **一週內的事件視覺強調**：太遠的不強調——全部都強調等於沒強調。

**已知限制（誠實記錄）**：沒有總經數據（CPI、GDP、景氣對策信號）的發布時程，
查證後確認無免費結構化來源；沒有個股法說會日期（同第十節的限制）；
人工清單需要每年手動更新，系統只能提醒你過期，沒辦法自己去抓。

---

## 十六之二、大盤總覽（M37 擴充，`indices` 欄位）

首頁往下第一個區塊。使用者的要求很明確：**台股優先**，另外要看得到
日經、韓國、費半與美股。

### 追蹤的六個指數

設定在 `config/settings.yaml` 的 `indices`，每一項有 `ticker` / `name` /
`short`，其中一項標 `primary: true`：

| key | 指數 | 短名 |
|---|---|---|
| `twii` | 台股加權指數（**primary**） | 台股 |
| `sox` | 費城半導體指數 | 費半 |
| `spx` | 美國標普500 | 標普 |
| `ixic` | 那斯達克指數 | 那指 |
| `n225` | 日經225指數 | 日經 |
| `ks11` | 韓國綜合指數 | 韓股 |

台股標 `primary` 不只是排序問題——前端據此決定給它兩倍的視覺重量
（大卡 + 完整統計 + 文字分析），其餘五檔排網格。**主角是誰要由資料說了算，
不是寫死在前端的 if 判斷裡**，否則之後要換主指數得改前端。

### 每個指數輸出什麼

```
indices.<key> = {
  name, short, primary,
  price, change_pct,
  history: [...]           # 近 60 個交易日收盤價(analysis.index_history_days)
  stats: {
    change_5d_pct,         # 近5日漲跌
    change_20d_pct,        # 近20日漲跌
    position_in_range_pct, # 目前價位在這 60 日高低區間的哪個位置(0~100)
    above_ma20,            # 是否站上月線(true/false/None)
  },
  analysis: "..."          # 白話趨勢描述,由 describe_index_trend() 產生
}
```

`position_in_range_pct` 是為了回答「現在算高還低」——單看漲跌幅看不出來。
`above_ma20` 可能是 `None`：資料不足 20 天時不猜，前端顯示「月線資料不足」
而不是預設 false。**「不知道」跟「跌破」是兩件事**，混在一起會讓人誤判。

`history` 給的是收盤價陣列，前端畫成行內 SVG sparkline。用 SVG 而非
Chart.js：六張小圖各開一個 Chart.js 實例的成本，遠高於它們承載的資訊量。

### 邊界

- 六個指數全部來自 yfinance。台股以外的指數有時差，`price` 是各自市場
  最後收盤價，**不是同一個時間點的快照**——跨市場比較當日漲跌幅時要記得
- 抓取失敗時該指數整筆缺席，前端不會為它捏造 0，畫面上就是少一張卡

---

## 十七、目前明確的缺口（建議補強方向）

> 本節在 M39 校正過一次。先前列的多項「沒有」其實在 M27/M33 已經做掉，
> 文件沒跟著改。**過期的缺口清單比沒有清單更糟**——它會讓人去做已經做完
> 的事，或以為系統的能力比實際差。以下是重新核對過的狀態。

技術面：
- ~~沒有 MACD、KD、布林通道~~ → **M27 已補**，見
  `src/analytics/indicators.py`：EMA、MACD、ADX（Wilder 平滑）、
  布林通道 + BB_WIDTH、KDJ、動量、Donchian、量能異常、Volume Profile POC
- 仍沒有：OBV、均線多頭排列 / 黃金交叉死亡交叉的明確判斷、
  跳空缺口與長黑長紅等 K 線型態識別
- RSI 仍用簡單移動平均，非標準 Wilder 平滑（ADX 有用 Wilder，RSI 沒有——
  這是不一致，補的時候要一起處理，否則兩個指標的平滑方式不同會很難解釋）

基本面：
- ~~沒有本益比、股價淨值比~~ → **M33 已補**（`src/data/valuation.py`，
  來源 TWSE `BWIBBU_ALL`），含殖利率。注意該來源約 23% 的個股 PE 為空字串，
  程式一律轉 `None` 而非 0——**把「不知道」寫成 0 會讓便宜的篩選條件
  把它們全撈進來**，那是會害到人的錯，不是顯示問題
- 仍沒有：EPS、ROE、ROA、負債比、自由現金流。查證結論見 `ROADMAP.md` M33
  「尚未做的部分」：`t187ap06/07_L_ci` 回的是「當日新公布」而非全市場快照，
  要全覆蓋得每天抓、累積存檔跨整個財報季
- `BWIBBU_ALL` 只涵蓋**上市**（TWSE），不含上櫃（TPEx）
- 毛利率只有 M10 全市場路徑沒有（月營收API不含毛利率），追蹤清單路徑
  （目前未使用）仍算得出毛利率但用季對季，見第九節「已知不一致」

籌碼面：
- **完全沒有** — 三大法人買賣超、融資融券、借券、大戶持股比例全都沒接

其他：
- 沒有產業分類 / 同業比較
- 沒有恐慌指數(VIX類)等總經波動指標，Market Regime(第十一節)只用價格
  結構判斷，不含總經數據
- MA5 / MA10 / MA60 已計算但未使用
- Paper Trading / Live Validation 已上線（第四節子章節，M21），但只到
  EOD（收盤）模擬層級；真正即時、含盤中觸價與滑價模擬的版本需要即時
  報價源 + 模擬撮合引擎，是獨立的系統工程，仍未開始（見 `MASTER_PLAN.md` Phase 3）
- 沒有減資、法人說明會的結構化事件資料(第十節「已知限制」)，證交所
  OpenAPI 不提供這兩項

---

## 十八、資料來源現況

| 來源 | 取得內容 | 穩定性 |
|---|---|---|
| yfinance | 個股歷史 OHLCV、指數、季報財務 | 中等，偶爾抓不到需重試 |
| 證交所 OpenAPI | 全市場當日行情、月營收年增率、除權除息預告、每日重大訊息 | 中等，格式偶有調整，已做3次重試 |

籌碼面資料（三大法人等）需另接資料源，證交所有公開 API 但尚未串接。

證交所 OpenAPI 已查證**沒有**減資、法人說明會的專屬結構化資料表
（2026-08-04 直接呼叫 API 驗證），這兩項只存在於重大訊息的自由文字裡，
見第十節「已知限制」。
